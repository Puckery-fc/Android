<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sample.event.yinteli.loginexample" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FriendsActivity" />
        <activity
            android:name=".SimpleAdatperActivity"
            android:label="@string/title_activity_simple_adatper" >
        </activity>
        <activity
            android:name=".BaseAdapterActivity"
            android:label="@string/title_activity_base_adapter" >
        </activity>
    </application>

</manifest>
